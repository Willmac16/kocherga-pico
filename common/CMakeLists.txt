#[[
	This source tree is used for common code which can be shared across
	multiple different platforms. 
]]

add_subdirectory(crossCoreCan2040)

# add_library(common-types INTERFACE)
# target_include_directories(common-types INTERFACE types nunavut)
